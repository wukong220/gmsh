#include "../edge_swap/helpers.h"
#include <iostream>

// Skeleton tests for smoothVertex-style behaviors. These are independent (do not
// call into gmsh) and intended as a place to encode deterministic cases.

int main(){
  std::cout << "smoothVertex_unit_tests: skeleton - no checks performed\n";
  return 0;
}
